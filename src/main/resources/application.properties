# =======================================================
#   CONFIGURAÇÕES PARA ORACLE (FIAP)
# =======================================================

spring.datasource.url=jdbc:oracle:thin:@oracle.fiap.com.br:1521/orcl
spring.datasource.username=${DB_USERNAME:rm557177}
spring.datasource.password=${DB_PASSWORD:170403}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway DESLIGADO por enquanto (banco criado pelo script)
spring.flyway.enabled=false

# OUTRAS CONFIGS
spring.cache.type=simple
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE
api.security.token.secret=easy-moto-mi-sonaria

server.port=8081
